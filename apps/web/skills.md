always use philosopher icons
keep minimal clean
structure the project into parts in components so it has reusable components and a good structure of fivided code too not everythning in one page.tsx
always use hooks to make be calls, and shoe error success using toasts in the hooks themselves
never show inline success and error messages
dont use useeffect and usestate